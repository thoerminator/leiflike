/**
 * Die öffentliche Adresse der Seite — für absolute Links in
 * Vorschaubildern, robots.txt und Sitemap.
 * Auf dem Server per NEXT_PUBLIC_SITE_URL überschreibbar.
 */
export const siteUrl = process.env.NEXT_PUBLIC_SITE_URL || "https://leiflike.de";
